// for

for(let index = 0; index < 10; index++){
    const element = index;
    console.log(element);
}
for (let index = 0; index < 6; index++) {
    for (let jndex = 0; jndex < 8; jndex++) {
        const element = jndex;
        console.log(element);
    }
    const element = index
    console.log(element);
    
}